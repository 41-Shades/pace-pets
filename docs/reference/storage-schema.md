# Storage Schema

Status: reference.

Pace Pets stores durable history in `chrome.storage.local`. The latest safe refresh status is stored separately so the dashboard can show recent collection outcomes even when history is unchanged.

<!-- storage-schema:generated:start -->

## Generated Reference

_Generated by `node scripts/storage-schema-doc.mjs --write` from `data/usage.sample.json`, `collector/extension/usage-windows.js`, `collector/extension/usage-values.js`, `collector/extension/refresh-status.js`, and `collector/extension/history-store.js`._

### History Shape

```json
{
  "historyVersion": 1,
  "samples": [
    {
      "id": "2026-05-21T14:32:00-07:00",
      "collectedAt": "2026-05-21T21:32:00.000Z",
      "source": "codex-wham-extension-background",
      "collectorVersion": "1.0.1",
      "windows": {
        "weekly": {
          "remainingPercent": 75,
          "usedPercent": 25,
          "resetsAt": "2026-05-26T18:32:00.000Z",
          "windowMinutes": 10080
        },
        "fiveHour": {
          "remainingPercent": 68,
          "usedPercent": 32,
          "resetsAt": "2026-05-22T00:32:00.000Z",
          "windowMinutes": 300
        }
      }
    }
  ]
}
```

### Refresh Status Shape

```json
{
  "ok": true,
  "message": "Usage unchanged; history already current.",
  "authFailure": false,
  "statusCode": null,
  "refreshedAt": "2026-05-21T21:37:00.000Z",
  "sampleCount": 1,
  "stored": false
}
```

### Runtime Constants

- Storage key: `codexUsageHistory`
- Refresh status key: `codexUsageRefreshStatus`
- History version: `1`
- Retention: `14` days
- Sample cap: `500` samples
- Plateau sample interval: `30` minutes
- Selected-window preference key: `codex-usage-window`
- Default selected window: `weekly`
- Supported selected-window values: `weekly`, `fiveHour`

### Supported Usage Windows

- `weekly`: 10080 minutes, `7d` badge, Week title, weekly chart label
- `fiveHour`: 300 minutes, `5h` badge, 5h title, 5-hour chart label

<!-- storage-schema:generated:end -->

## Local Preferences

The selected usage window is stored in `chrome.storage.local` under the generated selected-window preference key so the dashboard, background toolbar badge, and badge-view context menu share the same local preference. Supported values, labels, and durations are listed in the generated section above from `collector/extension/usage-windows.js`.

The dashboard theme is stored in extension-page `localStorage` under `codex-usage-theme`. These preferences are not usage history and do not contain account identifiers.

## Refresh Status

The generated refresh status shape above tracks the latest background check outcome so the dashboard can distinguish a recent check from the latest stored chart sample.

Refresh status contains only check metadata and sanitized status text. It must not contain raw upstream responses, auth material, or account identifiers.

## Stored Fields

- `id`: sanitized sample identifier, currently based on collection time.
- `collectedAt`: ISO timestamp for when the sample was collected.
- `source`: sanitized collector/source marker.
- `collectorVersion`: sanitized extension version marker.
- `windows.<key>`: normalized usage window for one supported key from the generated supported-window table.
- `remainingPercent`: bounded `0..100` percent remaining, preserving source precision without derived floating-point residue.
- `usedPercent`: derived `100 - remainingPercent`, preserving decimal-safe calculation precision.
- `resetsAt`: ISO reset timestamp.
- `windowMinutes`: normalized window duration in minutes.

## Normalization Rules

- Samples without a valid collection timestamp are discarded.
- Windows without valid `remainingPercent`, `resetsAt`, and `windowMinutes` are discarded.
- Windows whose `windowMinutes` do not match the canonical duration for their supported key are discarded.
- Percent values are bounded to `0..100`; display surfaces choose their own rounding.
- Text markers are whitespace-normalized and capped at 80 characters.
- Stored history is deduped by sample `id`, sorted by `collectedAt`, pruned to retention, compacted for unchanged plateaus, and capped at the newest 500 samples.

## Forbidden Data

Do not store:

- cookies
- auth headers
- access tokens or session tokens
- raw upstream responses
- raw HTML or page text
- screenshots
- account identifiers
- cross-device or cross-user sync data
